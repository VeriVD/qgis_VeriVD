name: 🧪 Run Tests
on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

env:
  QGIS_TEST_VERSION: 3.44


jobs:
  qgis_deps_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: docker test
        run: |
          docker run -v $(pwd):/usr/src qgis/qgis:${QGIS_TEST_VERSION} /usr/src/.docker/run-docker-tests.sh
    
